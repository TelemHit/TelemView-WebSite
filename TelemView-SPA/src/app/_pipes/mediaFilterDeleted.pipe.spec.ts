/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { MediaFilterDeletedPipe } from './mediaFilterDeleted.pipe';

describe('Pipe: MediaFilterDeletede', () => {
  it('create an instance', () => {
    let pipe = new MediaFilterDeletedPipe();
    expect(pipe).toBeTruthy();
  });
});
